import { useLocation, Link } from 'react-router-dom';
import '../styles/analysis.css';

const AnalysisResult = () => {
  const location = useLocation();
  
  // Extract results from location state, or use high-fidelity default data if none exists
  const analysisData = location.state?.result || {
    quality_score: 78,
    green_score: 85,
    full_report: {
      complexity: 'O(N)',
      energy_impact: 'Low',
      recommendations: [
        'Reduce nested loops',
        'Use generators where possible',
        'Avoid redundant calculations',
        'Optimize memory allocation'
      ]
    },
    report: 'This code exhibits good structural pattern practices with minimal waste loop executions. However, utilizing generator functions for large datasets will further minimize high memory occupancy peaks.'
  };

  const { quality_score, green_score, full_report, report } = analysisData;
  const complexity = full_report?.complexity || 'O(N)';
  const energyImpact = full_report?.energy_impact || 'Low';
  const recommendations = full_report?.recommendations || [
    'Reduce nested loops',
    'Use generators where possible',
    'Avoid redundant calculations',
    'Optimize memory allocation'
  ];

  return (
    <div className="analysis-container" style={{ maxWidth: '1000px', margin: '0 auto', padding: '40px 20px' }}>
      <div className="result-header" style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '32px' }}>
        <div>
          <h1 style={{ fontSize: '32px', fontWeight: '500' }}>Sustainability Report</h1>
          <p className="subtitle" style={{ color: 'var(--text-muted)', marginTop: '8px' }}>Generated by IBM Granite AI</p>
        </div>
        <Link to="/history" className="btn-secondary" style={{ textDecoration: 'none', padding: '10px 20px', borderRadius: '8px' }}>
          View All Reports
        </Link>
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))', gap: '20px', marginBottom: '32px' }}>
        {/* Code Quality Score */}
        <div className="score-card" style={{ background: 'var(--bg-card)', border: '1px solid rgba(255,255,255,0.07)', padding: '24px', borderRadius: '16px', textAlign: 'center' }}>
          <span style={{ fontSize: '14px', color: 'var(--text-muted)', display: 'block', marginBottom: '12px' }}>Code Quality Score</span>
          <div style={{ fontSize: '48px', fontWeight: '700', color: '#b9adf0', marginBottom: '8px' }}>{quality_score}</div>
          <p style={{ fontSize: '12px', color: 'var(--text-muted)' }}>Measures maintainability and coding practices.</p>
        </div>

        {/* Green Score */}
        <div className="score-card" style={{ background: 'var(--bg-card)', border: '1px solid rgba(255,255,255,0.07)', padding: '24px', borderRadius: '16px', textAlign: 'center' }}>
          <span style={{ fontSize: '14px', color: 'var(--text-muted)', display: 'block', marginBottom: '12px' }}>Green Score</span>
          <div style={{ fontSize: '48px', fontWeight: '700', color: '#86efac', marginBottom: '8px' }}>{green_score}</div>
          <p style={{ fontSize: '12px', color: 'var(--text-muted)' }}>Measures sustainability and energy efficiency.</p>
        </div>

        {/* Time Complexity */}
        <div className="score-card" style={{ background: 'var(--bg-card)', border: '1px solid rgba(255,255,255,0.07)', padding: '24px', borderRadius: '16px', textAlign: 'center' }}>
          <span style={{ fontSize: '14px', color: 'var(--text-muted)', display: 'block', marginBottom: '12px' }}>Time Complexity</span>
          <div style={{ fontSize: '36px', fontWeight: '600', color: '#60a5fa', margin: '14px 0 14px' }}>{complexity}</div>
          <p style={{ fontSize: '12px', color: 'var(--text-muted)' }}>Estimated algorithm complexity.</p>
        </div>

        {/* Energy Impact */}
        <div className="score-card" style={{ background: 'var(--bg-card)', border: '1px solid rgba(255,255,255,0.07)', padding: '24px', borderRadius: '16px', textAlign: 'center' }}>
          <span style={{ fontSize: '14px', color: 'var(--text-muted)', display: 'block', marginBottom: '12px' }}>Energy Impact</span>
          <div style={{ display: 'flex', justifyContent: 'center', gap: '8px', margin: '22px 0 20px' }}>
            <span style={{ padding: '4px 8px', borderRadius: '4px', fontSize: '12px', fontWeight: '600', opacity: energyImpact.toLowerCase() === 'low' ? 1 : 0.25, background: 'rgba(34, 197, 94, 0.2)', color: '#22c55e' }}>Low</span>
            <span style={{ padding: '4px 8px', borderRadius: '4px', fontSize: '12px', fontWeight: '600', opacity: energyImpact.toLowerCase() === 'medium' ? 1 : 0.25, background: 'rgba(234, 179, 8, 0.2)', color: '#eab308' }}>Medium</span>
            <span style={{ padding: '4px 8px', borderRadius: '4px', fontSize: '12px', fontWeight: '600', opacity: energyImpact.toLowerCase() === 'high' ? 1 : 0.25, background: 'rgba(239, 68, 68, 0.2)', color: '#ef4444' }}>High</span>
          </div>
          <p style={{ fontSize: '12px', color: 'var(--text-muted)' }}>Resource-based power impact level.</p>
        </div>
      </div>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))', gap: '30px' }}>
        {/* Recommendations */}
        <div className="analysis-card" style={{ background: 'var(--bg-card)', border: '1px solid rgba(255, 255, 255, 0.07)', padding: '30px', borderRadius: '16px' }}>
          <h2 style={{ fontSize: '20px', fontWeight: '500', marginBottom: '20px' }}>Recommendations</h2>
          <ul style={{ listStyleType: 'none', paddingLeft: 0 }}>
            {recommendations.map((rec, index) => (
              <li key={index} style={{ color: 'var(--text-muted)', fontSize: '14px', marginBottom: '12px', display: 'flex', alignItems: 'flex-start' }}>
                <span style={{ color: 'var(--purple-accent)', marginRight: '8px', fontSize: '16px' }}>•</span>
                <span>{rec}</span>
              </li>
            ))}
          </ul>
        </div>

        {/* Summary Report */}
        <div className="analysis-card" style={{ background: 'var(--bg-card)', border: '1px solid rgba(255, 255, 255, 0.07)', padding: '30px', borderRadius: '16px' }}>
          <h2 style={{ fontSize: '20px', fontWeight: '500', marginBottom: '20px' }}>Summary Report</h2>
          <p style={{ color: 'var(--text-muted)', fontSize: '14px', lineHeight: '1.6' }}>{report}</p>
        </div>
      </div>

      <div style={{ marginTop: '30px', textAlign: 'center' }}>
        <Link to="/analysis/new" className="btn-cta" style={{ textDecoration: 'none', padding: '12px 24px', borderRadius: '8px', display: 'inline-block' }}>
          Analyze Another File
        </Link>
      </div>
    </div>
  );
};

export default AnalysisResult;

